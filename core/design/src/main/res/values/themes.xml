<resources xmlns:tools="http://schemas.android.com/tools">
    <bool name="lightNavEnabled">true</bool>

    <!-- Base application theme. -->
    <style name="Theme.com.fawadjmalik.marsexplorer" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/bg</item>
        <item name="colorPrimaryVariant">@color/bg</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/bg</item>
        <item name="android:windowLightStatusBar" >@bool/lightNavEnabled</item>
        <item name="android:navigationBarColor" tools:targetApi="o">?colorSurface</item>
        <item name="android:windowLightNavigationBar" tools:ignore="NewApi">@bool/lightNavEnabled
        </item>
    </style>

    <style name="Theme.com.fawadjmalik.marsexplorer.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.com.fawadjmalik.marsexplorer.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.com.fawadjmalik.marsexplorer.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>